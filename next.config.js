/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'casamento.photosites.com.br', 'demaismarketing.com.br'
    ]
}
}

module.exports = nextConfig

